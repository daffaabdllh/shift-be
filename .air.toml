# Config file for [Air](https://github.com/air-verse/air) in toml format

# Working directory
root = "."
tmp_dir = "tmp"

[build]
# Just plain old shell command. You could use `make` as well.
cmd = "go build -o ./tmp/main ./src/main.go"
# Binary file yields from `cmd`.
bin = "./tmp/main"
# Customize binary, can setup environment variables when run your app.
full_bin = "./tmp/main"
# Watch these filename extensions.
include_ext = ["go", "tpl", "tmpl", "html", "toml", "env"]
# Ignore these filename extensions or directories.
exclude_dir = ["assets", "tmp", "vendor"]
# Watch these directories if you specified.
include_dir = []
# Exclude files.
exclude_file = []
# Exclude specific regular expressions.
exclude_regex = ["_test.go"]
# Exclude unchanged files.
exclude_unchanged = false
# Follow symlink for directories
follow_symlink = false
# This log file places in your tmp_dir.
log = "air.log"
# Poll files of watch directories.
poll = false
# Poll interval (ms)
poll_interval = 0
# It's not necessary to trigger build each time file changes if it's too frequent.
delay = 1000 # ms
# Stop running old binary when build errors occur.
stop_on_error = true
# Send Interrupt signal before killing process (useful for Windows)
send_interrupt = false
# Delay before sending Kill signal
kill_delay = 500 # ms

[log]
# Show log time
time = false

[color]
# Customize each part's color. If no color found, use the default theme.
main = "magenta"
watcher = "cyan"
build = "yellow"
runner = "green"

[misc]
# Clean garbage on exit
clean_on_exit = true
